<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Bands</title>
  </head>
  <body>
    <h1>All Bands</h1>
    <form action="<%= new_band_url%>" method="get">
      <%= authenticate %>
      <input type="submit" name="create_band" value="Add a Band">
    </form><br><br>
      <ul>
      <% Band.all.each do |band| %>
        <li><a href="<%= band_url(band)%>"><%= band.name %></a></li>
      <% end %>
      </ul>
  </body>
</html>
